The way that operator[] works leads to a problem
that requires the use of the NO_VALUE constant.

----------------------------------------------------

Demo all the calls to the List default constructor
that happen when the hash table array is allocated,
and to the List destructor when the array is deleted.
